#include "logicaloperators.h"

void logical_operators()
{
    equals_operator();
}

void equals_operator()
{
    bool found;
    found == false;
    !found;
}

/*操作符”：!, <, <=, >, >=, ==, !=, &&, ||
操作符的结果是bool类型
*/
